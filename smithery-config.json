{
	"type": "object",
	"properties": {
		"api_key": {
			"type": "string",
			"title": "Evaluation key",
			"description": "Issued Slidingbox key (sbk_<id>.<hmac>) covering a fixed number of free reads. Leave blank if paying by wallet."
		},
		"private_key": {
			"type": "string",
			"title": "Base wallet private key",
			"description": "Wallet holding USDC on Base; each read costs $0.02 over x402. Use a wallet funded for this and nothing else."
		},
		"base_url": {
			"type": "string",
			"title": "Service URL",
			"default": "https://slidingbox.ai",
			"description": "Leave as-is unless you were told otherwise."
		}
	}
}
